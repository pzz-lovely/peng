    javax.swing.JMenu
    JMenu(String label)
    用给定标签构造一个菜单
    JMenuItem add(JMenuItem item)
    添加一个菜单项
    JMenItem add(Action a)
    用给定侗族将一个菜单项添加到菜单中，并返回这个菜单项。
    void addSeparator()
    将一个分隔符行(separatorLine)天骄到菜单栏中
    JMenuItem insert(JMenuItem menu,int index)
    将一个新菜单项(或子菜单)添加到菜单的指定位置
    JMenuItem insert(Action a,int index)
    用给定的动作在菜单的指定位置添加一个新菜单项
    void insertSeparator(int index)
    将一个分隔符添加到菜单中 。
    void remove(int index)
    void remove(JMenuItem item)
    从菜单中删除指定的菜单项

    javax.swing.JMenuItem
    JMenuItem(String label)
    用给定标签构造一个菜单项
    JMenuItem(Action a)
    为给定动作构造一个菜单项
    JMenuItem(String label,Icon icon)
    用给定标签和图标构造一个菜单项
    void setEnable(boolean b)


    javax.swing.AbstractButton
    void setAction(Action a)
    为这个按钮或菜单项设置动作
    void setHorizontalTextPosition(int pos)
    设置文本对应图标的水平位置
        pos SwingConstants.Right文本在图标的右侧或 SwingConstants.LEFT

    javax.swing.AbstractAction
    AbstractAction(String name,IcOon smallIcon)
    用给定的名字和图标的构造一个抽象的动作


    javax.swing.event.MenuListener
    void menuSelected(MenuEvent e)
    在菜单被选择但尚未打开之前调用
    void menuDeselected(MenuEvent e)
    在菜单被取消选择的并且以及关闭的之后调用
    void menuCanceled(MenuEvent e)
    当菜单被取消的时被调用。例如用户点击菜单以外的区域